#include "main.h"

int _islower(int c)
{
	c = 0;
	return (c);
}

int _isalpha(int c)
{
	c = 0;
	return (c);
}

int _abs(int c)
{
	c = 0;
	return (c);
}

int _strlen(char *s)
{
	printf("%s\n", s);
	return (0);
}
