#include "main.h"

/**
 * add - adds two integers and return result
 * @n: first number
 * @m: second number
 * Return: @result if success
 */
int add(int n, int m)
{
	int result;

	result = n + m;
	return (result);
}
