#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t, int value);
void print_arr(int *, size_t, size_t);
#endif

