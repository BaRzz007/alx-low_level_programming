CC = gcc
CFLAG = -Wall -pedantic -Werror -Wextra -std=gnu89
SRC = 0-main.c 0-binary_to_uint.c
OBJ = $(patsubst %.c, %.o, $(SRC))

all: a

a: $(OBJ)
	$(CC) $(CFLAG) $(OBJ) -o a

%.o: %.c
	$(CC) -c $^ -o $@

Clean: 
	rm *.o


